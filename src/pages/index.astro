---
// Pre-launch: site is parked on the Holding page while the full marketing
// site is rebuilt. To restore, swap <Holding /> back to the full section
// stack (Nav, Hero, About, Faq, Newsletter, Footer) — components are
// untouched on disk.
import Layout from "../layouts/Layout.astro";
import Holding from "../components/Holding.astro";
---

<Layout>
  <Holding />
</Layout>
